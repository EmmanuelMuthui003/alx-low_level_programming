#include "main.h"

/**
 * main - Print _putchar
 *
 * Description - Program prints _putchar
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char prtText[8] = "_putchar";
	int length, i = 0;

	for (i = 0; i <= length; i++)
	{
		_putchar(prtText[i]);
	}
	putchar('\n');

	return (0);
}
